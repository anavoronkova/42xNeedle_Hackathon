# Public Tests

The public test suite will be released here at **20:00 on Thursday, May 21, 2026**.

After release, this folder will contain public test inputs, expected outputs, and metadata used by the public scorer.

Public tests are for iteration. They are not the final judge.

The hidden tests will use the same specification, but different combinations. Because memorizing public tests is not engineering, it is cosplay with JSON.
